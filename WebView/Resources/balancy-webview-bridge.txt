const balancy = {};

(function() {
    const pendingRequests = {};
    let requestCounter = 0;

    // Caching for images and localization
    const imageCache = {};
    const localizationCache = {};
    const pendingImageRequests = {};
    const pendingLocalizationRequests = {};

    const RequestAction = {
        None: 0,
        GetProfile: 1,
        SetProfile: 2,
        GetDocument: 3,
        GetLocalization: 10,
        GetImageUrl: 11,
        GetInfo: 12,
        CanBuyGroupOffer: 13,

        GetBattlePassProgress: 20,
        GetCustomEventInfo: 21,
        SetCustomEventInfo: 22,
        GetTasksInfoForTheEvent: 23,
        ActivateTasksForTheEvent: 24,
        DeactivateTasksForTheEvent: 25,
        ClaimTaskReward: 26,

        GetInventoryItemsCount: 30,
        AddInventoryItems: 31,
        RemoveInventoryItems: 32,

        BuyOffer: 101,
        BuyGroupOffer: 102,
        BuyShopSlot: 103,
        BattlePassClaim: 104,

        CloseWindow: 200,
        BalancyIsReady: 201,
        CustomMessage: 1000,
        BatchRequest: 399,  // Batch request for multiple requests
    };

    const ItemType = {
        Currency: 1 << 0,
        Item: 1 << 1,
        Event: 1 << 2,
    };

    const PriceType = {
        Hard: 0,
        Soft: 1,
        Ads: 2,
    };

    const TaskStatus = {
        None: 0,
        InProgress: 1,
        Completed: 2,
        Claimed: 3,
        Failed: 4,
    };

    const InfoType = {
        None: 0,
        OfferPrice: 1,
        OfferGroupPrice: 2,
        TimeLeft: 3,
        OwnerLocalizedKey: 4,
        OwnerCustom: 5,
        CustomPrice: 9,
        Custom: 10
    };

    const NotificationType = {
        OnNewEventActivated: 100,
        OnEventDeactivated: 101,
        OnNewOfferActivated: 102,
        OnOfferDeactivated: 103,
        OnNewOfferGroupActivated: 104,
        OnOfferGroupDeactivated: 105,

        OnShopSlotWasPurchased: 120,
        OnOfferWasPurchased: 121,
        OnOfferGroupWasPurchased: 122,
    };

    balancy.NotificationType = NotificationType;
    balancy.PriceType = PriceType;
    balancy.ItemType = ItemType;
    balancy.TaskStatus = TaskStatus;

    // Batch Manager - collects requests and sends them as a single batch
    class BatchManager {
        constructor() {
            this.pendingBatch = new Map(); // requestId -> {requestData, resolve, reject}
            this.batchTimeout = null;
            this.BATCH_DELAY = 16; // 16ms delay before sending batch
        }

        /**
         * Add request to current batch
         */
        addToBatch(sender, action, params, resolve, reject) {
            const requestId = (requestCounter++).toString();

            const requestData = {
                type: 'request',
                sender,
                id: requestId,
                action: action,
                params: params
            };

            this.pendingBatch.set(requestId, {
                requestData,
                resolve,
                reject
            });

            this.scheduleBatch();
            return requestId;
        }

        /**
         * Schedule batch sending if not already scheduled
         */
        scheduleBatch() {
            if (this.batchTimeout) return;

            this.batchTimeout = setTimeout(() => {
                this.sendBatch();
            }, this.BATCH_DELAY);
        }

        /**
         * Send all accumulated requests as a single batch
         */
        sendBatch() {
            if (this.pendingBatch.size === 0) {
                this.batchTimeout = null;
                return;
            }

            // Collect all requests as array
            const requests = [];
            this.pendingBatch.forEach((batchItem, requestId) => {
                requests.push(batchItem.requestData);
                // Store the promise handlers in global pendingRequests for response handling
                pendingRequests[requestId] = {
                    resolve: batchItem.resolve,
                    reject: batchItem.reject,
                    timestamp: Date.now()
                };
            });

            console.log(`[BatchManager] Sending batch with ${this.pendingBatch.size} requests`);

            // Create batch message
            const batchMessage = {
                type: 'batch',
                requests: requests
            };

            // Send the batch
            balancy.postMessage(JSON.stringify(batchMessage));

            // Clear current batch
            this.pendingBatch.clear();
            this.batchTimeout = null;
        }

        /**
         * Get current batch statistics
         */
        getStats() {
            return {
                pendingCount: this.pendingBatch.size,
                isScheduled: this.batchTimeout !== null
            };
        }
    }

    // Initialize batch manager
    const batchManager = new BatchManager();

    // Store original sendRequest function
    const originalSendRequest = function(sender, action, params = {}) {
        return new Promise((resolve, reject) => {
            const requestId = (requestCounter++).toString();

            pendingRequests[requestId] = {
                resolve: resolve,
                reject: reject,
                timestamp: Date.now()
            };

            const message = {
                type: 'request',
                sender,
                id: requestId,
                action: action,
                params: params
            };

            balancy.postMessage(JSON.stringify(message));
        });
    };

    balancy.findDomElement = (dataId, parentElement) => {
        const element = (parentElement ?? document).querySelector(`[data-id=${dataId}]`);
        return element instanceof HTMLElement ? element : null;
    }

    balancy.getTasks = () => {
        return balancy.sendRequest(null, RequestAction.GetTasksInfoForTheEvent, {id: window.balancyViewOwner.unnyIdGameEvent}).then(result => {
            return JSON.parse(result);
        });
    }

    balancy.activateTasks = (tasks) => {
        return balancy.sendRequest(null, RequestAction.ActivateTasksForTheEvent, {id: window.balancyViewOwner.unnyIdGameEvent, tasks}).then(result => {
            return JSON.parse(result);
        });
    }

    balancy.deactivateTasks = (tasks) => {
        return balancy.sendRequest(null, RequestAction.DeactivateTasksForTheEvent, {id: window.balancyViewOwner.unnyIdGameEvent, tasks});
    }

    balancy.claimTaskReward = (id) => {
        return balancy.sendRequest(null, RequestAction.ClaimTaskReward, {id}).then(response => {
            return response && response === 'ok';
        });
    };

    balancy.getInventoryItemsCount = (id) => {
        return balancy.sendRequest(null, RequestAction.GetInventoryItemsCount, {id});
    };

    balancy.addInventoryItems = (id, count) => {
        return balancy.sendRequest(null, RequestAction.AddInventoryItems, {id, count});
    };

    balancy.removeInventoryItems = (id, count) => {
        return balancy.sendRequest(null, RequestAction.RemoveInventoryItems, {id, count});
    };

    balancy.getSystemProfileValue = (path) => {
        return balancy.sendRequest(null, RequestAction.GetProfile, {profile: "UnnyProfile", path});
    };

    balancy.getProfileValue = (profile, path) => {
        return balancy.sendRequest(null, RequestAction.GetProfile, {profile, path});
    };

    balancy.closeView = (source) => {
        return balancy.sendRequest(null, RequestAction.CloseWindow, {id: source});
    };

    balancy.getDocumentValue = (id, depth) => {
        return balancy.sendRequest(null, RequestAction.GetDocument, {id, depth}).then(result => {
            return JSON.parse(result);
        });
    };

    balancy.getCustomEventInfo = () => {
        return balancy.sendRequest(null, RequestAction.GetCustomEventInfo, {id: window.balancyViewOwner.instanceId}).then(result => {
            return result ? JSON.parse(result) : {};
        });
    };

    balancy.setCustomEventInfo = (dataJson) => {
        const data = JSON.stringify(dataJson);
        return balancy.sendRequest(null, RequestAction.SetCustomEventInfo, {id: window.balancyViewOwner.instanceId, data});
    };

    balancy.getBattlePassConfig = () => {
        return balancy.getDocumentValue(window.balancyViewOwner.unnyIdGameEvent, 3).then(result => {
            return result.config;
        });
    };

    balancy.getBattlePassProgress = () => {
        return balancy.sendRequest(null, RequestAction.GetBattlePassProgress, {id: window.balancyViewOwner.unnyIdGameEvent}).then(result => {
            return JSON.parse(result);
        });
    };

    balancy.claimBattlePassReward = (lineId, index) => {
        return balancy.sendRequest(null, RequestAction.BattlePassClaim, {id: window.balancyViewOwner.unnyIdGameEvent, lineId, index}).then(response => {
            return response && response === 'ok';
        });
    };

    balancy.getLocalizedText = async function(key) {
        // Check cache first
        if (localizationCache[key] !== undefined) {
            return localizationCache[key];
        }

        // Check if request is already pending
        if (pendingLocalizationRequests[key]) {
            return pendingLocalizationRequests[key];
        }

        // Create new request and cache it
        const promise = balancy.sendRequest(null, RequestAction.GetLocalization, {key})
            .then(result => {
                localizationCache[key] = result;
                delete pendingLocalizationRequests[key];
                return result;
            })
            .catch(error => {
                delete pendingLocalizationRequests[key];
                throw error;
            });

        pendingLocalizationRequests[key] = promise;
        return promise;
    };

    balancy.getImageUrl = async function(id) {
        // Check cache first
        if (imageCache[id] !== undefined) {
            return imageCache[id];
        }

        // Check if request is already pending
        if (pendingImageRequests[id]) {
            return pendingImageRequests[id];
        }

        // Create new request and cache it
        const promise = balancy.sendRequest(null, RequestAction.GetImageUrl, {id})
            .then(result => {
                imageCache[id] = result;
                delete pendingImageRequests[id];
                return result;
            })
            .catch(error => {
                delete pendingImageRequests[id];
                throw error;
            });

        pendingImageRequests[id] = promise;
        return promise;
    };

    balancy.sendIsReady = () => {
        return originalSendRequest(null, RequestAction.BalancyIsReady, {id: 'none'});
    };

    balancy.delayIsReady = () => {
        balancy.delayedIsReady = true;
    };

    balancy.canBuyGroupOffer = (index) => {
        return balancy.sendRequest(null, RequestAction.CanBuyGroupOffer, {instanceId: window.balancyViewOwner.instanceId, index});
    };

    async function prepareSprite(element, id) {
        element.classList.add('balancy-image-prepared');
        const imageUri = await balancy.getImageUrl(id);
        if (imageUri) {
            if (element.tagName === 'IMG')
                element.src = imageUri;
            else {
                if (element.hasAttribute('data-use-nineslice') && element.getAttribute('data-use-nineslice') === 'false')
                    element.style.backgroundImage = `url('${imageUri}')`;
                else
                    element.style.borderImageSource = `url('${imageUri}')`;
            }
        }
    }

    balancy.setImage = function(element, sprite) {
        if (!sprite?.id)
            return;
        element.setAttribute('data-image-id', sprite.id);
        prepareSprite(element, sprite.id);
    }

    balancy.postMessage = function(message) {
        // iOS/macOS WebKit implementation
        if (window.webkit && window.webkit.messageHandlers && window.webkit.messageHandlers.BalancyWebView) {
            if (typeof message !== 'string') message = JSON.stringify(message);
            window.webkit.messageHandlers.BalancyWebView.postMessage(message);
            return true;
        }

        // Android JavascriptInterface implementation
        if (window.BalancyWebView && window.BalancyWebView.sendMessageToUnity) {
            if (typeof message !== 'string') message = JSON.stringify(message);
            window.BalancyWebView.sendMessageToUnity(message);
            return true;
        }

        // Fallback to postMessage for web views that support it
        if (window.parent && window.parent !== window && window.parent.postMessage) {
            window.parent.postMessage({
                source: 'balancy-view',
                type: 'message',
                payload: message,
                timestamp: Date.now()
            }, '*');
            return true;
        }

        console.warn('BalancyWebView: No message handler available for platform');
        if (window.BalancyWebView && window.BalancyWebView.log) {
            window.BalancyWebView.log('warn', 'No message handler available for platform');
        }

        return false;
    };

    balancy._receiveMessageFromUnity = function(message) {
        if (window.BalancyWebView && window.BalancyWebView.log) {
            window.BalancyWebView.log('info', 'Received message from Unity: ' + message.substring(0, 100) + '...');
        } else {
            console.log('BalancyWebView: Received message from Unity:', message.substring(0, 100) + '...');
        }

        balancy.handleResponse(message);
    };

    // Replace sendRequest with batched version
    balancy.sendRequest = function(sender, action, params = {}) {
        // Some actions should not be batched (e.g., purchases, sensitive operations)
        const nonBatchableActions = [
            RequestAction.BuyOffer,
            RequestAction.BuyGroupOffer,
            RequestAction.BuyShopSlot,
            RequestAction.BattlePassClaim,
            RequestAction.CloseWindow,
            RequestAction.BalancyIsReady
        ];

        if (nonBatchableActions.includes(action)) {
            return originalSendRequest(sender, action, params);
        }

        // Add to batch
        return new Promise((resolve, reject) => {
            batchManager.addToBatch(sender, action, params, resolve, reject);
        });
    };

    // Export batch manager for debugging
    balancy.batchManager = batchManager;

    balancy.setLocalizedText = async function(element, key) {
        if (key) {
            const localizedText = await balancy.getLocalizedText(key);
            element.textContent = localizedText;
        }
        element.classList.add('balancy-text-prepared');
    }

    async function localizeAllElements() {
        const elements = document.querySelectorAll('[data-text-type="localized"]');

        const localizationPromises = Array.from(elements).map(async (element) => {
            if (element.classList.contains('balancy-text-prepared'))
                return;

            const key = element.getAttribute('data-localization-key');
            await balancy.setLocalizedText(element, key);
        });

        await Promise.all(localizationPromises);

        document.dispatchEvent(new CustomEvent('balancy-localization-complete'));
    }

    async function prepareAllFonts() {
        const elements = document.querySelectorAll('[data-font-id]');
        const fontsPromises = Array.from(elements).map(async (element) => {
            if (element.classList.contains('balancy-font-prepared'))
                return;

            const name = element.getAttribute('data-font-name');
            const id = element.getAttribute('data-font-id');
            const fontUri = await balancy.getImageUrl(id);
            balancy.injectFontFace(name, fontUri);
        });

        await Promise.all(fontsPromises);

        document.dispatchEvent(new CustomEvent('balancy-fonts-complete'));
    }

    async function prepareAllSprites() {
        const elements = document.querySelectorAll('[data-image-id]');
        const imagesPromises = Array.from(elements).map(async (element) => {
            if (element.classList.contains('balancy-image-prepared'))
                return;

            const id = element.getAttribute('data-image-id');
            await prepareSprite(element, id);
        });

        await Promise.all(imagesPromises);

        document.dispatchEvent(new CustomEvent('balancy-images-complete'));
    }

    function refreshButtonAvailability(button) {
        const actionIdStr = button.getAttribute('data-button-action');
        if (!actionIdStr)
            return;
        const actionId = parseInt(actionIdStr, 10);

        switch (actionId) {
            case RequestAction.BuyGroupOffer:
                const index = parseInt(button.getAttribute('data-index')) || 0;
                balancy.canBuyGroupOffer(index).then(result => {
                    button.disabled = !result;
                });
                break;
        }
    }

    balancy.buyGroupOffer = function(index, paramsJson = {}) {
        if (!window.balancyViewOwner || !window.balancyViewOwner.instanceId)
            return;
        if (!paramsJson)
            paramsJson = {};
        paramsJson.instanceId = window.balancyViewOwner.instanceId;
        paramsJson.index = index;

        return balancy.sendRequest('buyGroupOfferButton', RequestAction.BuyGroupOffer, paramsJson);
    }

    balancy.getProductInfo = function(productId) {
        return balancy.sendRequest('offerGroupPrice', RequestAction.GetInfo, {
            type: InfoType.CustomPrice,
            productId
        });
    }

    function balancyButtonClicked(button) {
        const actionIdStr = button.getAttribute('data-button-action');
        if (!actionIdStr) {
            console.warn('Button missing data-button-action attribute', button);
            return;
        }

        const actionId = parseInt(actionIdStr, 10);
        if (isNaN(actionId) || actionId === RequestAction.None) {
            console.warn('Invalid action ID:', actionIdStr, button);
            return;
        }

        const senderId = button.id || 'unknown-button';
        const paramsAttr = button.getAttribute('data-button-params');
        let paramsJson = {};

        if (paramsAttr && paramsAttr.trim() !== '') {
            try {
                paramsJson = JSON.parse(paramsAttr);
            } catch (error) {
                console.error('Error parsing button params JSON:', error, 'Raw params:', paramsAttr);
                // Dispatch error event but don't return - continue with empty params
                button.dispatchEvent(new CustomEvent('balancyButtonError', {
                    detail: { actionId, error: 'Invalid JSON in data-button-params', paramsAttr }
                }));
                paramsJson = {};
            }
        }

        switch (actionId) {
            case RequestAction.BuyOffer:
                paramsJson.instanceId = window.balancyViewOwner.instanceId;
                break;
            case RequestAction.BuyGroupOffer:
                paramsJson.instanceId = window.balancyViewOwner.instanceId;
                paramsJson.index = parseInt(button.getAttribute('data-index')) || 0;
                break;
            case RequestAction.BuyShopSlot:
                paramsJson.slotId = button.getAttribute('data-index') || 0;
                break;
        }

        const originalDisabled = button.disabled;
        button.disabled = true;
        button.classList.add('balancy-button-processing');

        balancy.sendRequest(senderId, actionId, paramsJson)
            .then(result => {
                console.log(`Button action ${actionId} executed successfully`, result);
                button.dispatchEvent(new CustomEvent('balancyButtonResponse', {
                    detail: { actionId, result, success: true, senderId }
                }));
            })
            .catch(error => {
                console.error(`Error executing button action ${actionId}:`, error);
                button.dispatchEvent(new CustomEvent('balancyButtonResponse', {
                    detail: { actionId, error: error.message, success: false, senderId }
                }));
            })
            .finally(() => {
                button.disabled = originalDisabled;
                button.classList.remove('balancy-button-processing');
            });
    }

    function prepareAllButtons() {
        const elements = document.querySelectorAll('[data-button-action]');
        let preparedCount = 0;

        elements.forEach(element => {
            if (element.classList.contains('balancy-button-prepared'))
                return;

            if (element._balancyClickHandler)
                element.removeEventListener('click', element._balancyClickHandler);

            element._balancyClickHandler = function(event) {
                event.preventDefault();
                event.stopPropagation();
                balancyButtonClicked(element);
            };

            refreshButtonAvailability(element);

            element.addEventListener('click', element._balancyClickHandler);

            element.classList.add('balancy-button-prepared');
            preparedCount++;
        });

        document.dispatchEvent(new CustomEvent('balancy-buttons-complete', {
            detail: { preparedCount, totalFound: elements.length }
        }));
    }

    function prepareAllTexts() {
        const elements = document.querySelectorAll('[data-text-type="dynamic"]');

        const promises = [];

        elements.forEach(el => {
            const element = el;
            if (element.classList.contains('balancy-text-prepared'))
                return;

            element.classList.add('balancy-text-prepared');

            const senderId = element.id || 'unknown-text';
            let type = parseInt(element.getAttribute('data-info-type')) || 0;
            const index = parseInt(element.getAttribute('data-index')) || 0;
            const custom = element.getAttribute('data-custom') || '';
            let productId = element.getAttribute('data-product-id') || '';
            let instanceId = '';

            if (type === InfoType.OfferPrice) {
                productId = window.balancyViewOwner.productId;
                type = InfoType.CustomPrice;
            } else if (type === InfoType.OfferGroupPrice) {
                instanceId = window.balancyViewOwner.instanceId;
            }

            if (type === InfoType.TimeLeft) {
                const timerElement = element;
                function setTimeValue() {
                    timerElement.textContent = balancy.formatTime(balancy.getTimeLeft());
                }
                setInterval(setTimeValue, 1000);
                setTimeValue();
                return;
            }

            if (type === InfoType.OwnerCustom) {
                const formatStr = element.getAttribute("data-text-format");
                balancy.formatDataTemplate(formatStr, window.balancyViewOwner).then(formattedText => {
                    element.textContent = formattedText;
                });
                return;
            }

            if (type === InfoType.OwnerLocalizedKey) {
                const formatStr = element.getAttribute("data-text-format");
                balancy.formatDataTemplate(formatStr, window.balancyViewOwner).then(formattedKey => {
                    const key = formattedKey;
                    return balancy.setLocalizedText(element, key);
                });
                return;
            }

            const promise = balancy.sendRequest(senderId, RequestAction.GetInfo, {
                type,
                productId,
                index,
                instanceId,
                custom,
            }).then(result => {
                try {
                    const formatStr = element.getAttribute("data-text-format");
                    balancy.formatDataTemplate(formatStr, result).then(formattedText => {
                        element.textContent = formattedText;
                    });
                } catch (error) {
                    console.error('Error formatting text:', error, 'Result:', result);
                }
            });

            promises.push(promise);
        });


        Promise.all(promises).then(() => {
            document.dispatchEvent(new CustomEvent('balancy-text-complete'));
        });
    }

    balancy.notificationReceived = function(data) {
        if (!data || typeof data !== 'object')
            return;

        console.log(">>>notificationReceived ", data);
        switch (data.type) {
            case NotificationType.OnOfferDeactivated:
            case NotificationType.OnOfferGroupDeactivated:
                if (window.balancyViewOwner.instanceId === data.id)
                    balancy.closeView('Offer deactivated');
                break;
            case NotificationType.OnOfferGroupWasPurchased:
                const elements = document.querySelectorAll(`[data-button-action="${RequestAction.BuyGroupOffer}"]`);
                elements.forEach(refreshButtonAvailability);
                break;
        }
        window.dispatchEvent(new CustomEvent('balancy-notification', { detail: data }));
    }

    balancy.initResponseHandler = function() {
        if (balancy._responseHandlerInitialized) return;

        balancy._responseHandlerInitialized = true;

        balancy.handleResponse = function(responseJson) {
            try {
                const response = JSON.parse(responseJson);

                if (response.type === 'response') {
                    const request = pendingRequests[response.id];
                    if (request) {
                        delete pendingRequests[response.id];
                        if (response.error)
                            request.reject(new Error(response.error));
                        else
                            request.resolve(response.result);
                    }
                } else if (response.type === 'batch-response') {
                    // Handle batch response - array of individual responses
                    if (response.responses && Array.isArray(response.responses)) {
                        response.responses.forEach(individualResponse => {
                            const request = pendingRequests[individualResponse.id];
                            if (request) {
                                delete pendingRequests[individualResponse.id];
                                if (individualResponse.error)
                                    request.reject(new Error(individualResponse.error));
                                else
                                    request.resolve(individualResponse.result);
                            }
                        });
                    }
                } else {
                    if (response.type === 'notification') {
                        const data = response.data ? JSON.parse(response.data) : null;
                        balancy.notificationReceived(data);
                    }
                }
            } catch (error) {
                console.error('Error handling response:', error);
            }
        };

        window.addEventListener('message', function(event) {
            if (event.source !== window.parent)
                return;

            if (event.source === window)
                return;

            const data = event.data;
            if (data && typeof data === 'object') {
                if (data.payload)
                    balancy.handleResponse(data.payload);
            }
        });

        (function waitForAllBalancyEvents() {
            const requiredEvents = new Set([
                'balancy-buttons-complete',
                'balancy-localization-complete',
                'balancy-text-complete',
                'balancy-images-complete',
                'balancy-fonts-complete',
            ]);

            const receivedEvents = new Set();

            function checkAllReceived(event) {
                receivedEvents.add(event.type);
                if ([...requiredEvents].every(e => receivedEvents.has(e))) {
                    console.log("ALL IS READY");
                    window.dispatchEvent(new CustomEvent("balancy-ready"));

                    if (!balancy.delayedIsReady)
                        balancy.sendIsReady();
                }
            }

            requiredEvents.forEach(eventName => {
                document.addEventListener(eventName, checkAllReceived, { once: true });
            });
        })();

        localizeAllElements();
        prepareAllFonts();
        prepareAllSprites();
        prepareAllButtons();
        prepareAllTexts();
    };

    balancy.getTimeLeft = function() {
        const timeNow = Math.floor(Date.now() / 1000);
        const timeSpend = timeNow - window.balancySettings.launchTime;
        return window.balancySettings.secondsLeft - timeSpend;
    }

    balancy.shortenCount = function (count) {
        if (count >= 1e9) {
            return `${parseFloat((count / 1e9).toFixed(1))}B`;
        } else if (count >= 1e6) {
            return `${parseFloat((count / 1e6).toFixed(1))}M`;
        } else if (count >= 1e3) {
            return `${parseFloat((count / 1e3).toFixed(1))}k`;
        }
        return count.toString();
    }

    balancy.formatItemsCount = function (itemWithAmount, shouldShortenCount = false) {
        const localizedKey = itemWithAmount.item.name;
        let isDuration = localizedKey?.startsWith("LIVEOPS_TEMPLATES/INFINITE_");

        const isCurrency = itemWithAmount.item.type === ItemType.Currency;

        let text;
        if (isDuration) {
            const durationInSecs = itemWithAmount.count;
            const hoursPart = Math.floor(durationInSecs / 3600);
            const minsPart = Math.floor((durationInSecs % 3600) / 60);
            text = `${hoursPart > 0 ? `${hoursPart}h ` : ''}${minsPart > 0 ? `${minsPart}m` : ''}`;
        } else {
            const shortCount = shouldShortenCount ? balancy.shortenCount(itemWithAmount.count) : itemWithAmount.count.toString();
            text = isCurrency ? `${itemWithAmount.count}` : `x${shortCount}`;
        }

        return {
            text,
            isDuration,
            isCurrency
        };
    }

    balancy.formatTime = function (seconds) {
        if (seconds > 3600 * 24 * 365) // 1 year
            return 'oo';

        if (window.customFormatTime)
            return window.customFormatTime(seconds);

        const days = Math.floor(seconds / 86400);
        const hours = Math.floor((seconds % 86400) / 3600);
        const minutes = Math.floor((seconds % 3600) / 60);
        const secs = seconds % 60;

        if (days > 0) {
            if (hours > 0)
                return `${days}d ${hours}h`;
            return `${days}d`;
        } else
            return `${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`;
    }

    balancy.formatDataTemplate = async function(formatStr, data) {
        function capitalize(str) {
            return str.charAt(0).toUpperCase() + str.slice(1);
        }

        async function getNestedValueAsync(obj, path) {
            const parts = path.split('.');
            let current = obj;

            for (let i = 0; i < parts.length; i++) {
                const key = parts[i].trim();
                if (current && current[key] !== undefined && current[key] !== null) {
                    current = current[key];
                } else {
                    const unnyIdKey = 'unnyId' + capitalize(key);
                    if (current && current[unnyIdKey] !== undefined) {
                        try {
                            const document = await balancy.getDocumentValue(current[unnyIdKey], 0);
                            if (i + 1 < parts.length) {
                                const remainingPath = parts.slice(i + 1).join('.');
                                return await getNestedValueAsync(document, remainingPath);
                            } else
                                return document;
                        } catch (error) {
                            console.error(`Failed to get document for ID ${current[unnyIdKey]}:`, error);
                            return undefined;
                        }
                    }
                    return undefined;
                }
            }
            return current;
        }

        if (!formatStr.includes('{') || !formatStr.includes('}'))
            return formatStr;

        const placeholders = [];
        const regex = /\{([^}]+)\}/g;
        let match;

        while ((match = regex.exec(formatStr)) !== null) {
            placeholders.push({
                fullMatch: match[0],
                path: match[1].trim(),
                index: match.index
            });
        }

        if (placeholders.length === 0)
            return formatStr;

        const valuePromises = placeholders.map(async (placeholder) => {
            const value = await getNestedValueAsync(data, placeholder.path);
            return {
                placeholder: placeholder.fullMatch,
                value: value !== undefined ? String(value) : placeholder.fullMatch
            };
        });

        const resolvedValues = await Promise.all(valuePromises);

        let result = formatStr;
        for (const resolved of resolvedValues)
            result = result.replace(resolved.placeholder, resolved.value);

        return result;
    };

    balancy.injectFontFace = function(fontName, localFontPath) {
        const style = document.createElement('style');
        style.type = 'text/css';

        style.textContent = `
            @font-face {
                font-family: '${fontName}';
                src: url('${localFontPath}') format('truetype');
            }
        `;
        document.head.appendChild(style);
    }

    balancy.initResponseHandler();
})();

window.balancy = balancy;