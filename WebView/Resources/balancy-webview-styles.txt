(function() {
    // Предотвращаем повторную инъекцию
    if (window.balancyPerformanceInjected) return;
    window.balancyPerformanceInjected = true;

    console.log('[BalancyWebView] Injecting universal performance optimizations for game-like WebView');

    // Универсальные CSS стили для игрового WebView
    function injectGameUIStyles() {
        if (document.getElementById('balancy-game-ui-styles')) return;

        var style = document.createElement('style');
        style.id = 'balancy-game-ui-styles';
        style.textContent = '
            /* === УНИВЕРСАЛЬНЫЕ СТИЛИ ДЛЯ ВСЕХ ПЛАТФОРМ === */
            
            /* Отключение выделения текста */
            * {
                -webkit-user-select: none !important;
                -moz-user-select: none !important;
                -ms-user-select: none !important;
                user-select: none !important;
                -webkit-touch-callout: none !important;
                -webkit-context-menu: none !important;
                outline: none !important;
                -webkit-user-drag: none !important;
                user-drag: none !important;
            }
            
            /* Курсор по умолчанию для всех текстовых элементов */
            body, div, span, p, h1, h2, h3, h4, h5, h6, a, button {
                cursor: default !important;
            }
            
            /* Отключение drag and drop для изображений */
            img {
                -webkit-user-drag: none !important;
                user-drag: none !important;
                pointer-events: none !important;
                -webkit-touch-callout: none !important;
            }
            
            /* Отключение автокоррекции и автозаполнения для input полей */
            input, textarea {
                -webkit-autocorrect: off !important;
                -webkit-autocapitalize: off !important;
                autocomplete: off !important;
                spellcheck: false !important;
                /* Предотвращаем zoom при фокусе на мобильных */
                font-size: 16px !important;
                -webkit-appearance: none !important;
                -moz-appearance: none !important;
                appearance: none !important;
            }
            
            /* Основные настройки body */
            body {
                -webkit-tap-highlight-color: transparent !important;
                overscroll-behavior: none !important;
                overscroll-behavior-x: none !important;
                overscroll-behavior-y: none !important;
                -webkit-text-size-adjust: 100% !important;
                -ms-text-size-adjust: 100% !important;
                text-size-adjust: 100% !important;
                touch-action: manipulation !important;
                -webkit-overflow-scrolling: auto !important;
                -webkit-user-drag: none !important;
                -ms-content-zooming: none !important;
                -ms-touch-action: manipulation !important;
                position: fixed !important;
                top: 0 !important;
                left: 0 !important;
                width: 100% !important;
                height: 100% !important;
                overflow: hidden !important;
            }
            
            /* html настройки для предотвращения скролла страницы */
            html {
                position: fixed !important;
                top: 0 !important;
                left: 0 !important;
                width: 100% !important;
                height: 100% !important;
                overflow: hidden !important;
                -ms-touch-action: manipulation !important;
                touch-action: manipulation !important;
                scrollbar-width: none !important;
                -ms-overflow-style: none !important;
            }
            
            /* Скрытие всех скроллбаров */
            ::-webkit-scrollbar {
                width: 0px !important;
                height: 0px !important;
                display: none !important;
                background: transparent !important;
            }
            
            ::-webkit-scrollbar-track {
                display: none !important;
            }
            
            ::-webkit-scrollbar-thumb {
                display: none !important;
            }
            
            /* Разрешаем скролл для элементов с определенными CSS свойствами */
            [style*="overflow: auto"], 
            [style*="overflow: scroll"], 
            [style*="overflow-y: auto"], 
            [style*="overflow-y: scroll"],
            [style*="overflow-x: auto"], 
            [style*="overflow-x: scroll"],
            .scrollable, 
            .scroll-container, 
            .overflow-auto, 
            .overflow-scroll {
                -webkit-overflow-scrolling: touch !important;
                touch-action: pan-y !important;
                overscroll-behavior: contain !important;
            }
            
            /* === iOS СПЕЦИФИЧНЫЕ СТИЛИ === */
            @supports (-webkit-touch-callout: none) {
                body {
                    -webkit-overflow-scrolling: auto !important;
                    -webkit-touch-callout: none !important;
                    -webkit-user-select: none !important;
                }
                
                /* Предотвращаем zoom при фокусе на input */
                input, textarea, select {
                    font-size: 16px !important;
                    transform: translateZ(0) !important;
                }
                
                /* iOS Safari specific */
                html {
                    -webkit-text-size-adjust: 100% !important;
                }
                
                /* Разрешаем скролл для скроллируемых элементов на iOS */
                [style*="overflow: auto"], 
                [style*="overflow: scroll"], 
                [style*="overflow-y: auto"], 
                [style*="overflow-y: scroll"],
                .scrollable, 
                .scroll-container, 
                .overflow-auto, 
                .overflow-scroll {
                    -webkit-overflow-scrolling: touch !important;
                    touch-action: pan-y !important;
                    overscroll-behavior: contain !important;
                }
            }
            
            /* === ANDROID СПЕЦИФИЧНЫЕ СТИЛИ === */
            @media screen and (-webkit-device-pixel-ratio: 1), 
                   screen and (-webkit-device-pixel-ratio: 1.5), 
                   screen and (-webkit-device-pixel-ratio: 2), 
                   screen and (-webkit-device-pixel-ratio: 3) {
                body {
                    overscroll-behavior-y: none !important;
                    -webkit-overflow-scrolling: auto !important;
                }
                
                input, textarea {
                    font-size: 16px !important;
                    -webkit-appearance: none !important;
                }
            }
            
            /* === DESKTOP СПЕЦИФИЧНЫЕ СТИЛИ === */
            @media (hover: hover) and (pointer: fine) {
                body {
                    overflow-x: hidden !important;
                    -ms-overflow-style: none !important;
                }
                
                /* Отключение контекстного меню для desktop */
                * {
                    -webkit-context-menu: none !important;
                    context-menu: none !important;
                }
            }
            
            /* === WEBGL СПЕЦИФИЧНЫЕ СТИЛИ === */
            .unity-webgl-canvas {
                body {
                    margin: 0 !important;
                    padding: 0 !important;
                    overflow: hidden !important;
                }
            }
            
            // /* === ОТКЛЮЧЕНИЕ АНИМАЦИЙ БРАУЗЕРА === */
            // *, *::before, *::after {
            //     -webkit-animation-duration: 0s !important;
            //     animation-duration: 0s !important;
            //     -webkit-transition-duration: 0s !important;
            //     transition-duration: 0s !important;
            //     -webkit-animation-delay: 0s !important;
            //     animation-delay: 0s !important;
            //     -webkit-transition-delay: 0s !important;
            //     transition-delay: 0s !important;
            // }
            
            /* === АНИМАЦИИ КНОПОК === */
            /* Переопределяем отключение анимаций для кнопок */
            button, input[type="button"], input[type="submit"], input[type="reset"], .btn, [role="button"] {
                -webkit-transition-duration: 0.1s !important;
                transition-duration: 0.1s !important;
                -webkit-transition-property: transform, -webkit-transform !important;
                transition-property: transform, -webkit-transform !important;
                -webkit-transition-timing-function: ease-out !important;
                transition-timing-function: ease-out !important;
                transform: scale(1) !important;
                -webkit-transform: scale(1) !important;
                transform-origin: center center !important;
                -webkit-transform-origin: center center !important;
                will-change: transform !important;
            }
            
            /* Анимация нажатия - Mouse/Touch Down */
            button:active, 
            input[type="button"]:active, 
            input[type="submit"]:active, 
            input[type="reset"]:active, 
            .btn:active, 
            [role="button"]:active {
                transform: scale(0.95) !important;
                -webkit-transform: scale(0.95) !important;
                -webkit-transition-duration: 0.05s !important;
                transition-duration: 0.05s !important;
            }
            
            /* Анимация отпускания - Mouse/Touch Up */
            button:not(:active), 
            input[type="button"]:not(:active), 
            input[type="submit"]:not(:active), 
            input[type="reset"]:not(:active), 
            .btn:not(:active), 
            [role="button"]:not(:active) {
                transform: scale(1) !important;
                -webkit-transform: scale(1) !important;
                -webkit-transition-duration: 0.1s !important;
                transition-duration: 0.1s !important;
            }
            
            /* Дополнительные селекторы для различных типов кнопок */
            a[role="button"], 
            div[role="button"], 
            span[role="button"],
            .button,
            .ui-button,
            .clickable {
                -webkit-transition-duration: 0.1s !important;
                transition-duration: 0.1s !important;
                -webkit-transition-property: transform, -webkit-transform !important;
                transition-property: transform, -webkit-transform !important;
                -webkit-transition-timing-function: ease-out !important;
                transition-timing-function: ease-out !important;
                transform: scale(1) !important;
                -webkit-transform: scale(1) !important;
                transform-origin: center center !important;
                -webkit-transform-origin: center center !important;
                will-change: transform !important;
            }
            
            a[role="button"]:active, 
            div[role="button"]:active, 
            span[role="button"]:active,
            .button:active,
            .ui-button:active,
            .clickable:active {
                transform: scale(0.95) !important;
                -webkit-transform: scale(0.95) !important;
                -webkit-transition-duration: 0.05s !important;
                transition-duration: 0.05s !important;
            }
            
            /* === СТИЛИ ДЛЯ DISABLED/ЗАБЛОКИРОВАННЫХ КНОПОК === */
            
            /* Disabled кнопки - отключение всех анимаций и взаимодействий */
            button:disabled,
            input[type="button"]:disabled,
            input[type="submit"]:disabled,
            input[type="reset"]:disabled,
            .btn:disabled,
            .btn.disabled,
            [role="button"][disabled],
            [role="button"].disabled,
            [role="button"][aria-disabled="true"],
            a[role="button"].disabled,
            a[aria-disabled="true"],
            div[role="button"].disabled,
            div[aria-disabled="true"],
            span[role="button"].disabled,
            span[aria-disabled="true"],
            .button:disabled,
            .button.disabled,
            .ui-button:disabled,
            .ui-button.disabled,
            .clickable:disabled,
            .clickable.disabled,
            .balancy-button-processing {
                /* Визуальное состояние */
                opacity: 0.5 !important;
                filter: grayscale(0.7) !important;
                cursor: not-allowed !important;
                
                /* Отключение всех взаимодействий */
                pointer-events: none !important;
                
                /* Отключение анимаций */
                transform: scale(1) !important;
                -webkit-transform: scale(1) !important;
                -webkit-transition: none !important;
                transition: none !important;
                
                /* Отключение hover эффектов */
                box-shadow: none !important;
                -webkit-box-shadow: none !important;
            }
            
            /* Отключение :active состояния для disabled кнопок */
            button:disabled:active,
            input[type="button"]:disabled:active,
            input[type="submit"]:disabled:active,
            input[type="reset"]:disabled:active,
            .btn:disabled:active,
            .btn.disabled:active,
            [role="button"][disabled]:active,
            [role="button"].disabled:active,
            [role="button"][aria-disabled="true"]:active,
            a[role="button"].disabled:active,
            a[aria-disabled="true"]:active,
            div[role="button"].disabled:active,
            div[aria-disabled="true"]:active,
            span[role="button"].disabled:active,
            span[aria-disabled="true"]:active,
            .button:disabled:active,
            .button.disabled:active,
            .ui-button:disabled:active,
            .ui-button.disabled:active,
            .clickable:disabled:active,
            .clickable.disabled:active,
            .balancy-button-processing:active {
                transform: scale(1) !important;
                -webkit-transform: scale(1) !important;
                -webkit-transition: none !important;
                transition: none !important;
                opacity: 0.5 !important;
                pointer-events: none !important;
            }
            
            /* Специальные стили для кнопок в процессе обработки */
            .balancy-button-processing {
                position: relative !important;
            }
            
            /* Опциональный loading индикатор для processing кнопок */
            .balancy-button-processing::after {
                content: '' !important;
                position: absolute !important;
                top: 50% !important;
                left: 50% !important;
                width: 16px !important;
                height: 16px !important;
                margin: -8px 0 0 -8px !important;
                border: 2px solid transparent !important;
                border-top: 2px solid currentColor !important;
                border-radius: 50% !important;
                animation: balancy-spin 1s linear infinite !important;
                -webkit-animation: balancy-spin 1s linear infinite !important;
            }
            
            @keyframes balancy-spin {
                0% { transform: rotate(0deg); }
                100% { transform: rotate(360deg); }
            }
            
            @-webkit-keyframes balancy-spin {
                0% { -webkit-transform: rotate(0deg); }
                100% { -webkit-transform: rotate(360deg); }
            }
            
            /* === ДОПОЛНИТЕЛЬНЫЕ ОПТИМИЗАЦИИ === */
            
            /* Отключение focus outline */
            *:focus {
                outline: none !important;
                -webkit-tap-highlight-color: transparent !important;
            }
            
            /* Отключение selection highlight */
            ::selection {
                background: transparent !important;
            }
            
            ::-moz-selection {
                background: transparent !important;
            }
            
            /* Предотвращение случайного масштабирования */
            html {
                -ms-touch-action: manipulation !important;
                touch-action: manipulation !important;
            }
            
            /* Отключение автозаполнения форм */
            input:-webkit-autofill,
            input:-webkit-autofill:hover,
            input:-webkit-autofill:focus {
                -webkit-box-shadow: 0 0 0 1000px transparent inset !important;
                -webkit-text-fill-color: inherit !important;
            }
        ';

        (document.head || document.documentElement).appendChild(style);
        console.log('[BalancyWebView] Game UI styles injected successfully');
    }

    // Функция для проверки, является ли элемент скроллируемым
    function isScrollableElement(element) {
        if (!element || element === document.body || element === document.documentElement) {
            return false;
        }
        
        var style = window.getComputedStyle(element);
        var overflow = style.overflow || style.overflowY || style.overflowX;
        
        return overflow === 'auto' || 
               overflow === 'scroll' || 
               element.classList.contains('scrollable') ||
               element.classList.contains('scroll-container') ||
               element.classList.contains('overflow-auto') ||
               element.classList.contains('overflow-scroll') ||
               element.scrollHeight > element.clientHeight ||
               element.scrollWidth > element.clientWidth;
    }

    // Функция для поиска ближайшего скроллируемого родителя
    function findScrollableParent(element) {
        var current = element;
        while (current && current !== document.body && current !== document.documentElement) {
            if (isScrollableElement(current)) {
                return current;
            }
            current = current.parentElement;
        }
        return null;
    }

    // Универсальные JavaScript оптимизации
    function applyGameUIBehavior() {
        // Отключение контекстного меню
        document.addEventListener('contextmenu', function(e) {
            e.preventDefault();
            e.stopPropagation();
            return false;
        }, { passive: false, capture: true });

        // Отключение выделения текста
        document.addEventListener('selectstart', function(e) {
            e.preventDefault();
            e.stopPropagation();
            return false;
        }, { passive: false, capture: true });

        // Отключение drag and drop
        document.addEventListener('dragstart', function(e) {
            e.preventDefault();
            e.stopPropagation();
            return false;
        }, { passive: false, capture: true });

        // iOS специфичные оптимизации
        if (/iPad|iPhone|iPod/.test(navigator.userAgent)) {
            console.log('[BalancyWebView] Applying iOS-specific optimizations');

            // Умная обработка touchmove для iOS
            document.addEventListener('touchmove', function(e) {
                // Разрешаем touchmove для полей ввода
                if (['INPUT', 'TEXTAREA'].includes(e.target.tagName)) {
                    return;
                }
                
                // Ищем ближайший скроллируемый родительский элемент
                var scrollableParent = findScrollableParent(e.target);
                
                if (scrollableParent) {
                    // Если найден скроллируемый родитель, разрешаем скролл
                    return;
                } else {
                    // Если скроллируемый родитель не найден, предотвращаем bounce эффект страницы
                    e.preventDefault();
                }
            }, { passive: false });

            // Предотвращение zoom при double tap
            var lastTouchEnd = 0;
            document.addEventListener('touchend', function(event) {
                var now = Date.now();
                if (now - lastTouchEnd <= 300) {
                    event.preventDefault();
                }
                lastTouchEnd = now;
            }, { passive: false });

            // Отключение callout
            document.addEventListener('touchstart', function(e) {
                e.target.style.webkitTouchCallout = 'none';
            }, { passive: true });
        }

        // Android специфичные оптимизации
        if (/Android/.test(navigator.userAgent)) {
            console.log('[BalancyWebView] Applying Android-specific optimizations');

            // Предотвращение системных жестов по краям экрана
            document.addEventListener('touchstart', function(e) {
                if (e.touches[0].clientX < 20 ||
                    e.touches[0].clientX > window.innerWidth - 20 ||
                    e.touches[0].clientY < 20 ||
                    e.touches[0].clientY > window.innerHeight - 20) {
                    e.preventDefault();
                }
            }, { passive: false });

            // Отключение pull-to-refresh только для основной страницы
            var startY = 0;
            document.addEventListener('touchstart', function(e) {
                startY = e.touches[0].pageY;
            }, { passive: true });

            document.addEventListener('touchmove', function(e) {
                // Проверяем, находимся ли мы внутри скроллируемого элемента
                var scrollableParent = findScrollableParent(e.target);
                
                if (!scrollableParent && e.touches[0].pageY > startY && window.pageYOffset === 0) {
                    e.preventDefault();
                }
            }, { passive: false });
        }

        // Desktop специфичные оптимизации
        if (!/Mobi|Android/i.test(navigator.userAgent)) {
            console.log('[BalancyWebView] Applying Desktop-specific optimizations');

            // Отключение клавиатурных сочетаний
            document.addEventListener('keydown', function(e) {
                // Отключаем Ctrl+A, Ctrl+C, Ctrl+V, Ctrl+S, Ctrl+P и т.д.
                if (e.ctrlKey || e.metaKey) {
                    var blockedKeys = ['a', 'c', 'v', 'x', 's', 'p', 'f', 'h', 'r', 'n', 'w', 't'];
                    if (blockedKeys.includes(e.key.toLowerCase())) {
                        e.preventDefault();
                        e.stopPropagation();
                        return false;
                    }
                }

                // Отключаем функциональные клавиши
                var blockedKeyCodes = [
                    112, 113, 114, 115, 116, 117, 118, 119, 120, 121, 122, 123, // F1-F12
                    116, // F5 (Refresh)
                    123  // F12 (Dev Tools)
                ];

                if (blockedKeyCodes.includes(e.keyCode)) {
                    e.preventDefault();
                    e.stopPropagation();
                    return false;
                }
            }, { passive: false, capture: true });

            // Отключение middle click scroll
            document.addEventListener('mousedown', function(e) {
                if (e.button === 1) { // Middle mouse button
                    e.preventDefault();
                    return false;
                }
            }, { passive: false });
        }

        console.log('[BalancyWebView] Game UI behavior applied successfully');
    }

    // Функция инициализации
    function initializeGameUI() {
        injectGameUIStyles();
        applyGameUIBehavior();

        // Добавляем meta viewport если его нет
        if (!document.querySelector('meta[name="viewport"]')) {
            var viewport = document.createElement('meta');
            viewport.name = 'viewport';
            viewport.content = 'width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, user-scalable=no, viewport-fit=cover';
            document.head.appendChild(viewport);
            console.log('[BalancyWebView] Viewport meta tag added');
        }

        console.log('[BalancyWebView] Game UI initialization completed');

        // Уведомляем о завершении инициализации
        document.dispatchEvent(new CustomEvent('balancyGameUIReady', {
            detail: {
                platform: /iPad|iPhone|iPod/.test(navigator.userAgent) ? 'iOS' :
                         /Android/.test(navigator.userAgent) ? 'Android' : 'Desktop',
                timestamp: Date.now()
            }
        }));
    }

    // Инициализируем когда DOM готов
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initializeGameUI);
    } else {
        initializeGameUI();
    }

})()